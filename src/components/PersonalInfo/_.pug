.content
  span ABOUT ME
  .img {{msg}}
  .personalInfo
    span 个性签名
  .articalList
    ul.artical-list
      li.artical-name(v-for='item in data')
        el-button(type='text')(:key='Math.random()') {{item.name}}
  .markerAera
    .span 热门评论区